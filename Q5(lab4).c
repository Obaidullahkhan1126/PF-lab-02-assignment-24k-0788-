#include<stdio.h>
int main(){
	int customer_id,unit,total_amount;
	float price,charge;
	char name;
	printf("Enter customer id /n,units,total_amount)
	switch (units){
		case unit < 200
		 charge = 16.20
		 break;
		 
		case unit >= 200 & unit < 300
		 charge = 20.10
		 break;
		 
		case unit >= 300 & unit < 500
		 charge = 27.10
		 break;
		 
		case unit =< 500
		 charge = 35.90
		 break;
	}
	total_amount= charge*unit
	if total_amount > 18000
	 total_amount = total amount *1.25
	 
	printf("The total amount is %d",total_amount)	
return 0;
		
		
		
	}
	