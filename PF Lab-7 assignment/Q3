#include<stdio.h>
int main(){
   int max=-1000000,min=1000000;
    int num[9];
    for(int i=0;i<9;i++){
    	printf("Enter number: ");
        scanf("%d, ",&num[i]);
        if (max <num[i]){
         max = num[i];
        }
        if (min>num[i]){
            min= num[i];
        }

    }
    printf("Maximum number: %d\n",max);
    printf("Minimum number: %d\n",min);
return 0;
}
