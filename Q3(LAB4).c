#include<stdio.h>
int main(){
	char character;
	printf("Enter a character");
	scanf(" %c",&character);
	IF character == a-c || character == A-C {
	THEN
	 IF character == a-c{ 
	 THEN
	  PRINT("Small alphabet");
	 ELSE
	  PRINT("Capital alphabet");
	 ENDIF
	 }
	ELSE
	 IF character = 0-9{
	 THEN
	  PRINT("Digit");
	 ELSE
	  PRINT("Special character");
	 ENDIF
	 }
	ENDIF
	}
	
return 0;
}
	  